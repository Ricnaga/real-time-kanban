{
  "name": "real-time-kanban",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @kanban/client dev",
    "build": "pnpm --filter @kanban/client build",
    "start": "pnpm --filter @kanban/client start",
    "server": "pnpm --filter @kanban/server dev",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.0.8",
    "oxlint": "^1.73.0",
    "prettier": "^3.9.4",
    "typescript": "^5"
  }
}
